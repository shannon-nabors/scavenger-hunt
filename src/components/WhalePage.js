import React from 'react'
import {Grid} from 'semantic-ui-react'

function WhalePage() {
  return (
    <div id="map-page">
      <Grid centered columns={2}>
        <Grid.Column>
          <div className="reg-container">
            You went to the whale, now add the letter on the rose.
          </div>
        </Grid.Column>
      </Grid>
    </div>
  )
}

export default WhalePage